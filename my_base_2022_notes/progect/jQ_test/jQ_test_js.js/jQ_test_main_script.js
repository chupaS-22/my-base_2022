//jQuery(document).ready(function(){
//здесь пишем наш код, который должен будет
//выполняться, только после загрузки web-страницы
//});
//jQuery(document).ready(function(){
//	$('.first').css({'color':'red'});
//});



function getDistance(speed, time) {
	let distance = speed * time;
	return distance;
	}
	let myDistance = getDistance(10, 5);